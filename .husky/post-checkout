#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo ✨ Post-checkout hook started...🚀
echo ✨ Checkout in progress...⏰
echo ✨ Installing packages...📦
start=$(date +%s)
npx pnpx pnpm install
end=$(date +%s)
echo "✨ Done in $(($end-$start))s."